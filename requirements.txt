# --- Config ---
pydantic>=2.6
PyYAML>=6.0

# --- Parser (Windows Event Log) ---
python-evtx>=0.7.4        # offline .evtx file parsing (do not depend on platform-specific modules)
pywin32>=306; sys_platform == "win32"   # live mode for Windows only

# --- Wazuh Integration ---
requests>=2.31            # Wazuh Indexer (OpenSearch) REST requests
urllib3>=2.0               # handling of self-signed certificates

# --- For future modules (already structured) ---
# scikit-learn>=1.4        # model/train.py, model/predict.py (Isolation Forest etc.)
# fastapi>=0.110            # api/app.py
# uvicorn>=0.29
# streamlit>=1.32           # dashboard/dashboard.py
